interface:
  display_name: "Repository Refresh"
  short_description: "Remove stale repository machinery and history"
  default_prompt: "Use $repo-refresh only for the explicitly requested repository-wide audit, cleanup, or verification; consolidate current truth, remove stale docs and proof machinery, and preserve unrelated work."
policy:
  allow_implicit_invocation: false
